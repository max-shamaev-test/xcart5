{##
 # Notification help
 #}
<div class="notification-help">
  <span>{{ t('Notification header, greeting, text and signature can be set using variables. A list of supported variables and their respective values is provided in the table below') }}:</span>
  <table>
    {% for variablePlaceholder, variableValue in this.getVariables() %}
      <tr>
        <td class="variable-placeholder">{{ variablePlaceholder }}</td>
        <td class="variable-value">{{ variableValue }}</td>
      </tr>
    {% endfor %}
  </table>
</div>
