{##
 # Version page
 #}

<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" href="{{ this.getStyleFileUrl() }}">
  <script src="{{ this.getScriptFileUrl() }}"></script>
</head>
<body>

<div id="version_page">

  <div class="version-message">
    {{ this.getVersionMessage() }}
  </div>

  <div class="license-message">
    {{ this.getLicenseMessage() }}
  </div>

  <div class="installation-message">
    {{ this.getInstallationMessage() }}
  </div>

  <div class="private-modules">
    <div class="header">
      {{ t('Private and custom modules:') }}
    </div>

    {% if this.getPrivateModules() %}
      <table class="sortable">
        <thead>
        <tr>
          <th>{{ t('Developer') }}</th>
          <th>{{ t('Module name') }}</th>
          <th>{{ t('Version') }}</th>
          <th>{{ t('Installation date') }}</th>
        </tr>
        </thead>
        <tbody>
        {% for module in this.getPrivateModules() %}
          <tr>
            <td>{{ module.getAuthorName() }}</td>
            <td>{{ module.getModuleName() }}</td>
            <td sorttable_customkey="{{ this.getModuleVersionSortableValue(module) }}">{{ module.getMajorVersion() }}.{{ module.getFullMinorVersion() }}</td>
            <td sorttable_customkey="{{ module.getDate() }}">{{ this.formatDate(module.getDate()) }}</td>
          </tr>
        {% endfor %}
        </tbody>
      </table>
    {% else %}
      {{ 'None' }}
    {% endif %}
  </div>
  <div class="public-modules">
    <div class="header">
      {{ t('Public installed modules:') }}
    </div>

    {% if this.getPublicModules() %}
      <table class="sortable">
        <thead>
        <tr>
          <th>{{ t('Developer') }}</th>
          <th>{{ t('Module name') }}</th>
          <th>{{ t('Version') }}</th>
          <th>{{ t('Installation date') }}</th>
        </tr>
        </thead>
        <tbody>
        {% for module in this.getPublicModules() %}
          <tr>
            <td>{{ module.getAuthorName() }}</td>
            <td>{{ module.getModuleName() }}</td>
            <td sorttable_customkey="{{ this.getModuleVersionSortableValue(module) }}">{{ module.getMajorVersion() }}.{{ module.getFullMinorVersion() }}</td>
            <td sorttable_customkey="{{ module.getDate() }}">{{ this.formatTime(module.getDate()) }}</td>
          </tr>
        {% endfor %}
        </tbody>
      </table>
    {% else %}
      {{ 'None' }}
    {% endif %}
  </div>

</div>
</body>
</html>
