{##
 # Category page
 #}

{% if this.getDescription() or this.isInInlineEditorMode() %}
<div class="category-description" data-inline-editable {{ this.printTagAttributes(this.getCategory().getFieldMetadata('description'))|raw }}>{{ this.getDescription()|raw }}</div>
{% endif %}
